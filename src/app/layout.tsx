import type { Metadata } from "next";
import { Playfair_Display, Inter } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  display: "swap",
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "ILEA Lifestyle Experience Series",
  description:
    "A curated lifestyle platform blending movement, wellbeing, and social connection into meaningful, premium experiences.",
  openGraph: {
    title: "ILEA Lifestyle Experience Series",
    description:
      "Movement → Connection → Transformation. Join curated experiences that inspire a more intentional way of living.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${playfair.variable} ${inter.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
